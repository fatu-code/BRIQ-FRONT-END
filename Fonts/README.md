# Fonts

Drop **`TAN-PARADISO.ttf`** here. It is wired via `@font-face` in `styles.css` and used
only for the lowercase `briq` wordmark. Until the file is present, the wordmark falls back
to Varela Round automatically, so nothing breaks.

Exact filename expected: `TAN-PARADISO.ttf`
